<%= simple_form_for @gb_repo, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :address %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                gb_repos_path, :class => 'btn' %>
  </div>
<% end %>
