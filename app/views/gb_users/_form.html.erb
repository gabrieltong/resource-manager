<%= simple_form_for @gb_user, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :username %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                gb_users_path, :class => 'btn' %>
  </div>
<% end %>
