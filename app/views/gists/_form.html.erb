<%= simple_form_for @gist, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :address %>
  <%= f.input :extra %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                gists_path, :class => 'btn' %>
  </div>
<% end %>
